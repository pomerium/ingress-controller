apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: pomerium
  labels:
    app.kubernetes.io/name: pomerium
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: pomerium
