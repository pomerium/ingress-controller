---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: namespaces.console.pomerium.io
spec:
  group: console.pomerium.io
  names:
    kind: Namespace
    listKind: NamespaceList
    plural: namespaces
    singular: namespace
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Namespace define Pomerium Enterprise Console namespace
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NamespaceSpec defines Pomerium Enterprise Console namespace
            properties:
              parentId:
                description: ParentID to create a new namespace below specified parent,
                  mutually exclusive with RefID.
                type: string
              refId:
                description: RefID to refer to an existing Pomerium namespace, mutually
                  exclusive with ParentID.
                type: string
              secret:
                description: Secret is a name of an Opaque Secret within same namespace,
                  that must have two fields token for Pomerium service account JWT
                  token, and endpoint containing URL to API gRPC endpoint.
                type: string
            type: object
          status:
            description: ResourceStatus represents the outcome of the latest attempt
              to reconcile it with Pomerium.
            properties:
              error:
                description: Error that prevented latest observedGeneration to be
                  synchronized with Pomerium.
                type: string
              observedAt:
                description: ObservedAt is when last reconciliation attempt was made.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration represents the .metadata.generation
                  that was last presented to Pomerium.
                format: int64
                type: integer
              reconciled:
                description: Reconciled is whether this object generation was successfully
                  synced with pomerium.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
